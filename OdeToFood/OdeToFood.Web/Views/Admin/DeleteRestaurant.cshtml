@model OdeToFood.Domain.Models.Restaurant

@{
    ViewBag.Title = "Delete Restaurant";
}


    <div class="text-center">
        <h2 class="text-center">Delete Restaurant</h2>

        <h3 class="text-center">Are you sure you want to delete this?</h3>

        <br />

        <div>

            @if (Model.ImagePath != null)
            {
                <img style="width:400px;" src="@Url.Content(Model.ImagePath)" alt="Restaurant logo">
            }
            else
            {
                <img style="width:400px;" src="~/Images/NoPhoto.jpg" alt="Restaurant logo">
            }

            <br />

            <div>
                <dl class="dl-horizontal">
                    <dt>
                        @Html.DisplayNameFor(model => model.RestaurantName)
                    </dt>

                    <dd>
                        @Html.DisplayFor(model => model.RestaurantName)
                    </dd>

                    <dt>
                        @Html.DisplayNameFor(model => model.Address)
                    </dt>

                    <dd>
                        @Html.DisplayFor(model => model.Address)
                    </dd>

                    <dt>
                        @Html.DisplayNameFor(model => model.Phone)
                    </dt>

                    <dd>
                        @Html.DisplayFor(model => model.Phone)
                    </dd>
                </dl>
            </div>

            <div class="text-center">
                @Html.ActionLink("Back to List", "IndexRestaurant")

                @using (Html.BeginForm())
                {
                    @Html.AntiForgeryToken()

                    <div class="form-actions no-color">
                        <input type="submit" value="Delete" class="btn btn-primary" />
                    </div>
                }

            </div>

        </div>
    </div>

